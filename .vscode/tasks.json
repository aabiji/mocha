{
    "tasks": [
        { // run with ctrl-shift-b
            "type": "cppbuild",
            "label": "run project",
            "command": "ninja && ./app ../assets/videos/earthquake.mp4",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "args": []
        }
    ],
    "version": "2.0.0"
}